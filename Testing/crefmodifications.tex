\documentclass{article}
\def \inside {words}
\def \list {\inside}
\begin{document}

\edef \list {\unexpanded\expandafter{\list \inside}}
\edef \list {\unexpanded\expandafter{\list \inside}}

\def \inside {fools}
\list

\end{document}